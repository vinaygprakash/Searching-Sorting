/*
#include <bits/stdc++.h> 
void bubbleSort(vector<int>& arr, int n)
{   
   for(int i=0;i<n-1;i++)
   {
       //no of loop it is n-1 times we have to do
       for (int j=0;j<n-1-i;j++)
           //this is for element
       if(arr[j]>arr[j+1])
       {
           swap(arr[j],arr[j+1]);
       }
   }
}


#include <bits/stdc++.h> 
void bubbleSort(vector<int>& arr, int n)
{   
   for(int i=1;i<n;i++)
   {
       //no of loop it is n-1 times we have to do
       for (int j=0;j<n-i;j++)
           //this is for element
       if(arr[j]>arr[j+1])
       {
           swap(arr[j],arr[j+1]);
       }
   }
}
*/